/**
 * Batch version of useFrozenWhileDragging — delegates to the same generic implementation.
 * Kept as a separate export for semantic clarity (single value vs. record of values).
 */
export { useFrozenWhileDragging as useFrozenBatch } from "./useFrozenWhileDragging";
